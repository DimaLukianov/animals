## Animals test app (angular + express)

You **need** install `node` and `npm`.

Then:

`$ cd project_name_dir`

`$ npm install`

`$ gulp`

Visit [localhost:3000](http://localhost:3000/).


*Author:*
** Dmytro Lukianov **
